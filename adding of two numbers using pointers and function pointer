#include<stdio.h>
int add(int *ptr1,int *ptr2, int sum)
{
    sum =*ptr1+*ptr2;
    printf("Adding of two numbers :%d",sum);
}
int main()
{
    int a=10,b=20;
    int *ptr1=&a;
    int *ptr2=&b;
    int sum;
    int (*fptr)(int *, int *, int)=add;
    fptr(ptr1,ptr2,sum);
}
